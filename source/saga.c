#include "saga.h"

void saga_add_log(saga_t* const saga, const size_t level, uint8_t* const log) {
    const size_t log_size = log[0];


}

saga_status_t saga_pull_logs(saga_t* const saga, uint8_t* const data, const size_t data_size, size_t* const data_out) {
    return 0;
}
